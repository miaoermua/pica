# Legacy Archive

This directory contains archived Bash implementations and related assets:

- `legacy/pica-cli/pica`
- `legacy/pica-pack/pica-pack`

These files are kept for reference and compatibility history.
Core active development is now focused on the Rust workspace under `crates/`.
